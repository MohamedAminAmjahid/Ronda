rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Avatars : lecture par tout utilisateur connecté, écriture réservée au propriétaire.
    match /avatars/{uid}.jpg {
      allow read: if request.auth != null;
      allow write: if request.auth.uid == uid;
    }
  }
}
